"""HZ-EV Brain AI models — LSTM / XGBoost+SHAP / Autoencoder / YOLOv8.

Each sub-package owns one model end-to-end (model definition, training,
evaluation, inference, saved weights).  The unified evaluation runner
lives in :mod:`ai.eval.benchmark`.
"""
